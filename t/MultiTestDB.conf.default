# This provides a default set of configurations which is merged with your
# provided MultiTestDB.conf.
#
# You should provide a MultiTestDB.conf which defines the databases

{
  'databases' => {
    'multi' => {
      'versioning' => 'Bio::EnsEMBL::DBSQL::DBAdaptor',
    },
  },
}
